apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: smartevent

resources:
  - configmap.yaml
  - secret.yaml
  - postgres-statefulset.yaml
  - ingress.yaml
  # Ajouter ici les deployments et services de Nizar

# Labels communs à toutes les ressources
commonLabels:
  app: smartevent
  environment: dev

# Images à utiliser (à remplacer par celles d'Issam)
# images:
#   - name: discovery-server
#     newTag: latest
