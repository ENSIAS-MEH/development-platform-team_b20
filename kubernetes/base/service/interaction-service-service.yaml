apiVersion: v1
kind: Service
metadata:
  name: interaction-service
  namespace: smartevent
spec:
  selector:
    app: interaction-service
  ports:
    - port: 8083
      targetPort: 8083
  type: ClusterIP